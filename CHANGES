1.6.0
1. Add group chat feature
2. Remove deeplinking as it's not used
3. Remove reset password feature as it's not used and adds complexity to the codebase

1.5.2
1. Dialog extra text + "Chat → Draw" renames in titles/content
2. Sidebar label renames
    Current	New
    Recent Chats	Recent Drawings
    Chat Requests	Draw Requests
    Saved Chats	Saved Drawings
3. Collapsible sidebar sections

1.5.1
======
1. Fix error where if user stays too long on an ad, reward is not granted.
2. Removed opened request from notification
3. Open the sidebar automatically when request from notification is opened.
4. Fix bug where the error "The other user has left the room" shows indefinitely. 
5. Do not close the chat window when the error shows.
6. Fix edge to edge display on Android 13+ devices.