name: passkeys_doctor
description: Local no-op override for passkeys_doctor to avoid bundling the Android doctor plugin.
publish_to: 'none'
version: 1.3.0+local

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
